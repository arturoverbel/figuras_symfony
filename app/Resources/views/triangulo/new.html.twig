{% extends 'base.html.twig' %}

{% block body %}
    <h1>Triángulo</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="btn-info" type="submit" value="Crear" />
    {{ form_end(form) }}

    <hr>
    <a href="{{ path('fig_index') }}">Lista de figuras</a>

{% endblock %}
