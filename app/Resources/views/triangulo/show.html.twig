{% extends 'base.html.twig' %}

{% block body %}
    <h1>Triangulo</h1>

    <table>
        <tbody>
            <tr>
                <th>Base</th>
                <td>{{ triangulo.base }}</td>
            </tr>
            <tr>
                <th>Altura</th>
                <td>{{ triangulo.altura }}</td>
            </tr>
            <tr>
                <th>Hipotenusa</th>
                <td>{{ triangulo.hipotenusa }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ triangulo.id }}</td>
            </tr>
            <tr>
                <th>Numlados</th>
                <td>{{ triangulo.numLados }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('triangulo_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('triangulo_edit', { 'id': triangulo.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
