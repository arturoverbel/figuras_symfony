{% extends 'base.html.twig' %}

{% block body %}
    <h1>Workspace</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ workspace.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ workspace.nombre }}</td>
            </tr>
            <tr>
                <th>Limitefiguras</th>
                <td>{{ workspace.limiteFiguras }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('workspace_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('workspace_edit', { 'id': workspace.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
