{% extends 'base.html.twig' %}

{% block body %}
    <h1>Hexagono</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Radio</th>
                <td>{{ hexagono.radio }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ hexagono.id }}</td>
            </tr>
            <tr>
                <th>Numlados</th>
                <td>{{ hexagono.numLados }}</td>
            </tr>
        </tbody>
    </table>



        <a href="{{ path('hexagono_index') }}">Back to the list</a>

            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}

{% endblock %}
